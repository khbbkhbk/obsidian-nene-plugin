{
  "name": "obsidian-nene-plugin-dev",
  "version": "1.0.0",
  "private": true,
  "description": "obsidian-nene-plugin development tooling",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "watch": "node ./scripts/build.mjs --watch",
    "dev": "node ./scripts/build.mjs --watch",
    "test": "node --test tests/graph-view-enhancer.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.6"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "monkey-around": "^2.3.0",
    "wildcard-match": "^5.1.4"
  }
}

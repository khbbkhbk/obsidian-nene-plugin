'use strict';

// 定义命令&URI增强模块的默认配置，当前仅保留文件夹末尾斜杠选项。
const DEFAULT_COMMAND_URI_ENHANCER_SETTINGS = {
  addTrailingSlashToFolders: false
};

module.exports = {
  DEFAULT_COMMAND_URI_ENHANCER_SETTINGS
};
